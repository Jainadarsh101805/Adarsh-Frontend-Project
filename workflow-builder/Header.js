import "./Header.css";

const Header = () => {
  return (
    <header className="app-header">
      <h1 className="app-title">Workflow Builder</h1>
    </header>
  );
};

export default Header;
